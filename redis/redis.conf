port 6379
bind 0.0.0.0
protected-mode no

# Cluster settings
cluster-enabled yes
cluster-config-file nodes.conf
cluster-node-timeout 5000

# Persistence
appendonly yes

# Security
aclfile /usr/local/etc/redis/users.acl
